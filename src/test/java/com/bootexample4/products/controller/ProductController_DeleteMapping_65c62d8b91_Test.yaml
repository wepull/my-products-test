# Test generated by RoostGPT for test azureopenai using AI Type Azure Open AI and AI Model Roost-gpt

config:
  target: "https://dummy-api.com"
  phases:
    - duration: 1
      arrivalRate: 10

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/product"
          json:
            id: 1
            name: "Test Product"
            price: 10.99
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 200
          - bodyContains: '"message":"Product deleted successfully"'
  
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/product"
          json:
            name: "Test Product"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 400
          - bodyContains: '"message":"Invalid payload. Please provide all required fields."'
    
  - name: "API request with edge case"
    flow:
      - post:
          url: "/product"
          jsonFile: "./path/to/large_payload.json"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode:
              min: 200
              max: 299
          - bodyMatches: "{ data: { id: Number, name: String, price: Number }}"
