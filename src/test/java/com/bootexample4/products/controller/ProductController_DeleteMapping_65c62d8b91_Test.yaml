# Test generated by RoostGPT for test jackdemo2 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"

scenarios:
  - name: "Successful delete request"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(1, 1000) }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
          # This scenario tests the successful deletion of a product. The product ID is a random number between 1 and 1000.
          # The expected status code is 200 and the response should have a "message" property.

  - name: "Delete request for non-existent product"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(10000, 20000) }}"
          expect:
            - statusCode: 404
          # This scenario tests the deletion of a product that does not exist. The product ID is a random number between 10000 and 20000.
          # The expected status code is 404 to indicate that the product was not found.

  - name: "Delete request with invalid product ID"
    flow:
      - delete:
          url: "/product/invalid_id"
          expect:
            - statusCode: 400
          # This scenario tests the deletion of a product with an invalid ID. The ID is a string "invalid_id".
          # The expected status code is 400 to indicate a bad request.

  - name: "Edge case: Delete request with large product ID"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(1, 1000000000000000) }}"
          expect:
            - statusCode: 404
          # This scenario tests the deletion of a product with a large ID. The product ID is a random number between 1 and 1 quadrillion.
          # The expected status code is 404 to indicate that the product was not found.
