# Test generated by RoostGPT for test demo830 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(1, 1000) }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200

  - name: "API request with non-existing product"
    flow:
      - delete:
          url: "/product/99999999999999999999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with invalid product id"
    flow:
      - delete:
          url: "/product/abc"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
