# Test generated by RoostGPT for test demoaug18bofa using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(1, 1000000) }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200

  - name: "API request with non-existent product"
    flow:
      - delete:
          url: "/product/{{ $randomNumber(1000001, 2000000) }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with invalid product ID"
    flow:
      - delete:
          url: "/product/invalidID"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
