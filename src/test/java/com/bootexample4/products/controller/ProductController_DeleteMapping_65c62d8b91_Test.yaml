# Test generated by RoostGPT for test ihordemo using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: <API_ENDPOINT_URL>
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: Successful API request
    flow:
      - post:
          url: "/{id}"
          headers:
            Content-Type: "application/json"
          json:
            id: 1
      - validate:
          status: 200
          json:
            id: 1

  - name: API request with validation failure
    flow:
      - post:
          url: "/{id}"
          headers:
            Content-Type: "application/json"
          json:
            # Invalid payload with missing required fields
      - validate:
          status: 400
          # Assert the error message in the response body

  - name: API request with edge case
    flow:
      - post:
          url: "/{id}"
          headers:
            Content-Type: "application/json"
          json:
            # Edge case payload
      - validate:
          # Assert response status code range
          # Assert the expected pattern or behavior in the response body
