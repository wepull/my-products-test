# Test generated by RoostGPT for test aug10demo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - log: "Deleting a product"
      - delete:
          url: "/2"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                message: "Product deleted successfully"
                
  - name: "API request with non-existing product"
    flow:
      - log: "Trying to delete a non-existing product"
      - delete:
          url: "/9999"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
                error: "Product not found"

  - name: "API request with invalid product ID"
    flow:
      - log: "Trying to delete a product with invalid ID"
      - delete:
          url: "/abc"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid product ID"
