# Test generated by RoostGPT for test june27demo using AI Model gpt

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    # Expecting a successful deletion and a 200 status code
    flow:
      - delete:
          url: "/2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200

  - name: "API request with non-existing product"
    # Expecting a 404 status code as the product does not exist
    flow:
      - delete:
          url: "/9999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with invalid product id"
    # Expecting a 400 status code as the product id is not valid
    flow:
      - delete:
          url: "/abc"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400