# Test generated by RoostGPT for test july4offbusinessdemo using AI Model gpt

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
          # The product with id 2 should be deleted successfully

  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/9999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
          # The product with id 9999 does not exist, so the deletion should fail

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/specialchar&*^%"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
          # The product id contains special characters, so the request should fail

  - name: "API request with large data"
    flow:
      - delete:
          url: "/12345678901234567890"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
          # The product with a large id does not exist, so the deletion should fail
