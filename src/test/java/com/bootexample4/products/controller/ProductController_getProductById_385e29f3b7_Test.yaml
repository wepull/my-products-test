# Test generated by RoostGPT for test LGDemoJuly23 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get: "/1"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "price"
    # Scenario: Successful API request. The API should return a 200 status code with a valid product object.

  - name: "API request for non-existent product"
    flow:
      - get: "/99999"
        expect:
          - statusCode: 404
    # Scenario: API request for non-existent product. The API should return a 404 status code.

  - name: "API request with invalid ID"
    flow:
      - get: "/abc"
        expect:
          - statusCode: 400
    # Scenario: API request with invalid ID. The API should return a 400 status code.

  - name: "API request with large ID"
    flow:
      - get: "/99999999999999999999999999999999999999999999999999"
        expect:
          - statusCode: 400
    # Scenario: API request with large ID. The API should return a 400 status code.

  - name: "API request with special character ID"
    flow:
      - get: "/@#$"
        expect:
          - statusCode: 400
    # Scenario: API request with special character ID. The API should return a 400 status code.
