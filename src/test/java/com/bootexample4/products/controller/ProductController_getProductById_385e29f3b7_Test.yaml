# Test generated by RoostGPT for test july4offbusinessdemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/1"
          expect:
            - statusCode: 200
            - hasProperty: "product"
  - name: "API request with non-existent product"
    flow:
      - get:
          url: "/99999"
          expect:
            - statusCode: 404
  - name: "API request with invalid ID"
    flow:
      - get:
          url: "/abc"
          expect:
            - statusCode: 400
  - name: "API request with edge case of large ID"
    flow:
      - get:
          url: "/99999999999999999999"
          expect:
            - statusCode: 404
