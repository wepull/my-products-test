# Test generated by RoostGPT for test july4offbusinessdemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/api/products"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            # Assuming that the response should contain an array of 4 products
            - equals: 
                products:
                  - count: 4

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/api/products"
          headers:
            # Assuming that an invalid header will result in a validation failure
            Invalid-Header: "invalid_value"
          expect:
            - statusCode: 400
            # Assuming that the response should contain an error message
            - hasProperty: "error"
            - equals: 
                error: "Invalid request headers"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/api/products"
          headers:
            # Assuming that a large header value is an edge case
            Large-Header: "{{ largeData }}"
          expect:
            - statusCode: "{{ statusCode }}"
            # Assuming that the response should match a certain pattern
            - matches: 
                products: "{{ pattern }}"
