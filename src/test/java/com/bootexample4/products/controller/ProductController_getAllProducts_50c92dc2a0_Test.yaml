# Test generated by RoostGPT for test LGDemoJuly23 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/api/products"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            - equals: 
                - "products.length": 4

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/products"
          json:
            name: ""
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                - "error.message": "Product name is required."

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/products"
          json:
            name: "Product with special characters: !@#$%^&*()_+"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "product"
            - equals: 
                - "product.name": "Product with special characters: !@#$%^&*()_+"
