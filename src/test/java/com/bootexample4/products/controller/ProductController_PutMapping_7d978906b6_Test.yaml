# Test generated by RoostGPT for test demoaug18bofa using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.endpoint.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/product/1"
          json:
            name: "Test Product"
            description: "This is a test product"
            price: 100
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/product/1"
          json:
            name: ""
            description: "This is a test product"
            price: 100
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - equals:
                - "message"
                - "Validation failed: name is required"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/product/1"
          json:
            name: "Test Product with special characters !@#$%^&*()"
            description: "This is a test product with a very long description. This is a test product with a very long description. This is a test product with a very long description."
            price: 1000000000
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
