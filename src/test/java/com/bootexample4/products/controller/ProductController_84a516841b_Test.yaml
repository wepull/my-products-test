# Test generated by RoostGPT for test july4offbusinessdemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.io/api/v1"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
            key2: "value2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "expectedProperty"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
            key2: "really long string .................."
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: {"$between": [200, 500]}
            - hasProperty: "expectedProperty"
