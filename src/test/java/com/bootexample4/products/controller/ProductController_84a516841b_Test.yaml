# Test generated by RoostGPT for test july4offbusinessdemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummy-api-endpoint.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
            key2: "value2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "expectedResponseKey"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/endpoint"
          json:
            key1: "value1"
            key2: "veryLargeValueOrSpecialCharacters"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ 200 | 400 }}"
            - hasProperty: "expectedResponseKey"
