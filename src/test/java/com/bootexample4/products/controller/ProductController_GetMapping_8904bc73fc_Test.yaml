# Test generated by RoostGPT for test ihordemo using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api-endpoint.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/{id}"
          json:
            id: 1
          set:
            headers:
              Content-Type: "application/json"
          capture:
            request:
              headers: true
          expect:
            statusCode: 200
            json:
              id: 1
              name: "Product 1"
              
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/{id}"
          json:
            invalidField: "Invalid Value"
          set:
            headers:
              Content-Type: "application/json"
          expect:
            statusCode: 400
            json:
              errorMessage: "Invalid payload. Required field 'id' is missing."
              
  - name: "API request with edge case"
    flow:
      - post:
          url: "/{id}"
          json:
            largeData: "Lorem ipsum dolor sit amet, consectetur adipiscing elit..."
          set:
            headers:
              Content-Type: "application/json"
          expect:
            statusCode:
              from: 200
              to: 299
            json:
              data: "Large data successfully processed."
