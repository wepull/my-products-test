# Test generated by RoostGPT for test demoaug18bofa using AI Type Open AI and AI Model gpt-4

config:
  target: 'http://api.example.com'
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: 'Successful API request'
    flow:
      - get:
          url: "/product/{{ $randomNumber(1, 100) }}"
          expect:
            - statusCode: 200
            - hasProperty: 'productName'

  - name: 'API request with non-existing product ID'
    flow:
      - get:
          url: "/product/9999999"
          expect:
            - statusCode: 404

  - name: 'API request with invalid product ID'
    flow:
      - get:
          url: "/product/abc"
          expect:
            - statusCode: 400
