# Test generated by RoostGPT for test june9rockwelldemo using AI Model gpt

config:
  target: "https://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Successful API request"
    flow:
      - log: "Testing successful API request"
      - get:
          url: "/products/{{ $randomNumber(1, 100) }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "description"

  - name: "API request with non-existent ID"
    flow:
      - log: "Testing API request with non-existent ID"
      - get:
          url: "/products/999999"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 404

  - name: "API request with invalid ID"
    flow:
      - log: "Testing API request with invalid ID"
      - get:
          url: "/products/invalid_id"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case: large ID"
    flow:
      - log: "Testing API request with edge case: large ID"
      - get:
          url: "/products/{{ $randomNumber(1000000, 9999999) }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 404