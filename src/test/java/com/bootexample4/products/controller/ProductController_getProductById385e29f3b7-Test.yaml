# Test generated by RoostGPT for test june27demo using AI Model gpt

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/1"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "price"
          - equals:
              id: 1

  - name: "API request with non-existing ID"
    flow:
      - get:
          url: "/999"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 404

  - name: "API request with edge case - large ID"
    flow:
      - get:
          url: "/1000000000000000"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 404

  - name: "API request with edge case - negative ID"
    flow:
      - get:
          url: "/-1"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case - non-numeric ID"
    flow:
      - get:
          url: "/abc"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 400