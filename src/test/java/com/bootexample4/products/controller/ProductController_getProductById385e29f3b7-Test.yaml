# Test generated by RoostGPT for test june29demo using AI Model vertex

scenario('Successful API request') {
  payload = {
    id: 1
  }
  headers = {
    'Content-Type': 'application/json'
  }
  response = http.get('http://localhost:8080/api/products/1', headers: headers, body: payload)
  assert response.status == 200
  assert response.body == {
    id: 1,
    name: 'Product 1',
    price: 10.00
  }
}

scenario('API request with validation failure') {
  payload = {
    id: 2
  }
  headers = {
    'Content-Type': 'application/json'
  }
  response = http.get('http://localhost:8080/api/products/2', headers: headers, body: payload)
  assert response.status == 400
  assert response.body == {
    'message': 'Product not found'
  }
}

scenario('API request with edge case') {
  payload = {
    id: 'abc'
  }
  headers = {
    'Content-Type': 'application/json'
  }
  response = http.get('http://localhost:8080/api/products/abc', headers: headers, body: payload)
  assert response.status == 400
  assert response.body == {
    'message': 'Invalid product ID'
  }
}