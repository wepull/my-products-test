# Test generated by RoostGPT for test june28demovertex using AI Model vertex

scenarios:
  - name: Successful API request
    request:
      method: PUT
      url: /api/products/{id}
      headers:
        Authorization: Bearer {token}
      body:
        {
          "name": "New product name",
          "description": "New product description",
          "price": 100.00
        }
    response:
      status_code: 200
      body:
        {
          "id": {id},
          "name": "New product name",
          "description": "New product description",
          "price": 100.00
        }
  - name: API request with validation failure
    request:
      method: PUT
      url: /api/products/{id}
      headers:
        Authorization: Bearer {token}
      body:
        {
          "name": "",
          "description": "New product description",
          "price": 100.00
        }
    response:
      status_code: 400
      body:
        {
          "errors": [
            {
              "code": "REQUIRED_FIELD_MISSING",
              "message": "The name field is required."
            }
          ]
        }
  - name: API request with edge case
    request:
      method: PUT
      url: /api/products/{id}
      headers:
        Authorization: Bearer {token}
      body:
        {
          "name": "New product name",
          "description": "New product description",
          "price": "100.00"
        }
    response:
      status_code: 200
      body:
        {
          "id": {id},
          "name": "New product name",
          "description": "New product description",
          "price": 100.00
        }