# Test generated by RoostGPT for test eondemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "data.csv"
    fields:
      - "id"
      - "name"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/getAllProducts"
          headers:
            Authorization: "Bearer validToken"
          expect:
            - statusCode: 200
            - hasProperty: "products"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/getAllProducts"
          headers:
            Authorization: "Bearer invalidToken"
          expect:
            - statusCode: 400
            - bodyContains:
                - "Invalid token"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/getAllProducts"
          headers:
            Authorization: "Bearer edgeCaseToken"
          expect:
            - statusCode: "{{ statusCodeInRange }}"
            - bodyContains:
                - "Edge case behavior"
