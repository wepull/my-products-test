# Test generated by RoostGPT for test ihordemo using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "http://dummy-api-endpoint.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/products"
          json:
            payload:
              field1: "value1"
              field2: "value2"
          headers:
            Authorization: "Bearer token"
      - expect:
          statusCode: 200
          json:
            - key: "field1"
              value: "value1"
            - key: "field2"
              value: "value2"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/products"
          json:
            payload:
              field1: "value1"
          headers:
            Authorization: "Bearer token"
      - expect:
          statusCode: 400
          json:
            - key: "error"
              value: "Missing required field"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/products"
          json:
            payload: "Special characters or large data"
          headers:
            Authorization: "Bearer token"
      - expect:
          statusCode:
            min: 200
            max: 299
          jsonPattern:
            - key: "response"
              pattern: "/^.*special.*$/"
