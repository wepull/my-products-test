# Test generated by RoostGPT for test jackdemo2 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/getAllProducts"
          expect:
            - statusCode: 200
            - hasProperty: 'products'

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/getAllProducts"
          headers:
            invalidHeader: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: 'error'

  - name: "API request with edge case"
    flow:
      - get:
          url: "/getAllProducts?param=veryLargeValue"
          expect:
            - statusCode: 200
            - hasProperty: 'products'
