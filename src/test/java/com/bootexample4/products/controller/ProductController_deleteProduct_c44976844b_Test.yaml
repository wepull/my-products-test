# Test generated by RoostGPT for test July13demo4 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete: "/2"
        expect:
          - statusCode: 200
        # The API should return a 200 status code on successful deletion

  - name: "API request with validation failure"
    flow:
      - delete: "/9999"
        expect:
          - statusCode: 404
        # The API should return a 404 status code when trying to delete a non-existent product

  - name: "API request with edge case"
    flow:
      - delete: "/%20"
        expect:
          - statusCode: 400
        # The API should return a 400 status code when trying to delete a product with an invalid id
