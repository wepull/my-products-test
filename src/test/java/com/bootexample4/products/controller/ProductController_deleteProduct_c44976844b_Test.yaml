# Test generated by RoostGPT for test LGDemoJuly23 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
    # The API should return a 200 status code when a valid request is sent

  - name: "API request with non-existing product"
    flow:
      - delete:
          url: "/999999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
    # The API should return a 404 status code when trying to delete a non-existing product

  - name: "API request with invalid product ID"
    flow:
      - delete:
          url: "/invalid_id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
    # The API should return a 400 status code when an invalid product ID is provided

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/0"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
    # The API should return a 404 status code when trying to delete a product with ID 0 (edge case)
