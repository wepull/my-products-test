# Test generated by RoostGPT for test LGDemoJuly23 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - log: "Deleting product"
      - delete:
          url: "/2"
          headers:
            Content-Type: "application/json"
      - think: 5
      - assert:
          - statusCode: 200
          - body: "Product deleted successfully"

  - name: "API request with non-existing product"
    flow:
      - log: "Deleting non-existing product"
      - delete:
          url: "/9999"
          headers:
            Content-Type: "application/json"
      - think: 5
      - assert:
          - statusCode: 404
          - body: "Product not found"

  - name: "API request with invalid product id"
    flow:
      - log: "Deleting product with invalid id"
      - delete:
          url: "/abc"
          headers:
            Content-Type: "application/json"
      - think: 5
      - assert:
          - statusCode: 400
          - body: "Invalid product id"
