# Test generated by RoostGPT for test july4offbusinessdemo using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080/api/products"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful deletion of product"
    flow:
      - delete:
          url: "/2"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
    # This scenario tests if the product with id 2 is successfully deleted and a 200 status code is returned.

  - name: "Delete non-existing product"
    flow:
      - delete:
          url: "/9999"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404
    # This scenario tests if a 404 status code is returned when trying to delete a product with a non-existing id.

  - name: "Delete product with invalid id"
    flow:
      - delete:
          url: "/abc"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
    # This scenario tests if a 400 status code is returned when trying to delete a product with an invalid id.

  - name: "Delete product without providing id"
    flow:
      - delete:
          url: "/"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 405
    # This scenario tests if a 405 status code is returned when trying to delete a product without providing an id.
