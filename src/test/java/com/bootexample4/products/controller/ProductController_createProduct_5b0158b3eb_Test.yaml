# Test generated by RoostGPT for test jackdemo2 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://your-api-url.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/product"
          json:
            name: "Test Product"
            price: 100
            description: "This is a test product"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/product"
          json:
            name: ""
            price: 100
            description: "This is a test product"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Product name is required"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/product"
          json:
            name: "Test Product with Special Characters !@#$%^&*()"
            price: 100000000000000000000000000000
            description: "This is a test product with really long description..."
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - equals: 
                name: "Test Product with Special Characters !@#$%^&*()"
